var chai = require('chai');
var assert = chai.assert;
var expect = chai.expect;
var should = chai.should;
var api = require("../app/controllers/api.controller");

/* NOT WORKING YET
it("greeting pass test", function() {
    let person = 'world';
    let result =  api.helloWorld(person);
    var checkObj = {
      "greeting": "Hello world!",
      "language": "en-GB",
      "person": "world"
    };
    expect(res).to.be.eql(checkObj);
    done();
});

describe("controller tests", function(){

    describe("greeting creation", function(){

    });

});
*/
